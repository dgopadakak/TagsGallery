# Всё, что нужно для сборки иконки, feature graphic и карточек скриншотов
pillow>=10.0

# Не требуется для карточек. Нужен, только если разбираете видео на кадры
# (проверка длительности, вытаскивание кадра). Ставится тяжело — при
# необходимости: pip install av
# av>=12.0
